When adding subtasks, only the first subtask is currently added successfully. Any subtasks added after that do not appear. Fix this so users can add any number of subtasks to the same task. The subtask list should update immediately after each new subtask is added.

The Delete button for subtasks currently does not work. Fix it so that clicking Delete removes the correct subtask from the task and immediately updates the displayed subtask list. The deletion must also be persisted correctly.