Add a way to rename tasks. Users should be able to change the title of an existing task directly from the task list.

The new title must not be empty. After renaming a task, the updated title should appear immediately and should still be there after restarting the application.
Renaming a task must not change its status, priority, tags, or any other task information.