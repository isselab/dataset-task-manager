Keep priority as the main ordering rule for open tasks. When two tasks have the same priority, show overdue tasks first, followed by tasks with the nearest due date.
Completed tasks should still stay below open tasks. Due dates must be saved between application restarts.