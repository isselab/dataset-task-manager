Refactor the task-query implementation so that keyword search and status filtering are no longer maintained as separate processing workflows.
Route both capabilities through the unified task-query mechanism and remove obsolete or duplicated search/filter logic that is no longer required.
Preserve the existing user-visible behavior and ensure that keyword-only queries, status-only queries, combined queries, and queries without either criterion continue to work correctly.